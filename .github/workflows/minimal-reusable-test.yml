name: minimal-reusable-test

on:
  workflow_call: {}

permissions:
  contents: read
  pull-requests: read

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello from reusable workflow"
